name: iosApp
options:
  bundleIdPrefix: com.aspigon1.mvdm
targets:
  iosApp:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources: [iosApp]
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.aspigon1.mvdm.iosApp.NKYQX69337
      INFOPLIST_FILE: iosApp/Info.plist
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      ARCHS: arm64
      VALID_ARCHS: arm64
